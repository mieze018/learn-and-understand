'use client';
import styled from 'styled-components';

export const Section1 = styled.section.attrs({
  className: 'flex flex-col gap-4 items-center w-full',
})``;

export const Section2 = styled.section.attrs({
  className: 'flex flex-col gap-4 items-center flex-1',
})``;

export const Section3 = styled.section.attrs({
  className: 'flex gap-4 items-end w-full',
})``;
